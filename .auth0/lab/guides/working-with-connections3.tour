{
  "$schema": "https://aka.ms/codetour-schema",
  "title": "3: Bulk import users to a database connection",
  "steps": [
    
    {
      "file": "",
      "line": 1,
      "title": "Prepare to use the Management API Explorer",
      "description": "## Add new users to the database using bulk migration\n\nAuth0 supports importing users from external applications using Custom Database connections, the Auth0 Management API, or the User Import/Export Extension.\n\nIn this exercise, you will perform a bulk user import using the Management API. Bulk imports are useful for migrating users from an existing database or service to Auth0. This extension is intended for simple use cases. For large bulk import jobs, writing a custom script that uses the Management API is recommended.  \n\n1. From the Auth0 Dashboard, go to **Applications** > **APIs**.\n    >The APIs list view is displayed. Notice that an API already exists for your tenant, the **Auth0 Management API**.  \n\n    >The Management API is a system API with an identifier of `https://{yourTenantName}.{yourRegion}.auth0.com/api/v2`.\n\n1. Select **Auth0 Management API**.\n>Note the warning alerting you to the fact this is an Auth0 API and cannot be modified or deleted.  \n\n1. Select the **Permissions** tab.\n >These are all the permissions that the Management API is granted.\n\n1. Select the **API Explorer** tab.\n >The API Explorer can be used to explore and prototype calls to the API without having to write a line of code. \n \n 1. If you've never used the Management API Explorer before, you will see a **Create & Authorize Test Application** button here. Select that button.\n    \n\n1. Next to the **Token** field, select the **Copy** icon to copy the value.\n>Note that this token includes all the scopes that are granted to the API Explorer Application, so by default it can invoke ALL the Management API endpoints. It should NOT be used on production environments. \n\n1. Review the instructions under the Using this token in the API Explorer section.\n\n1. Select the link listed in the first step of this section to open the Management API Explorer.\n",
      "selection": {
        "start": {
          "line": 87,
          "character": 50
        },
        "end": {
          "line": 87,
          "character": 107
        }
      }
    },
    {
      "title": "Import users using the Management API Explorer",
      "description": "## Import users using the Management API Explorer\n\n\n1. On the Management API Reference page, find and select **Jobs** from the left navigation menu.\n\n1. Select **Create import users job**.\n>  Looking at the documentation for this endpoint, you can see it requires a `POST` request to `/api/v2/jobs/users-imports` with an access token containing the `create:users` scope.  \n\n1. Select the **Try it** button.\n\n1. In the **tenantDomain** field, enter the URL of your tenant (e.g., `name.us.auth0.com`). You can locate this at the bottom of this browser tab. \n> **DO NOT** include `https://` in this field.\n\n1. In the **Authorization**, paste your token that you copied in the previous step next to **Bearer**.\n\n1. Examine the required Body parameters (the remaining parameters are optional): \n    - `connection_id`: the id of the connection you want to import the users into.\n    - `users`:  a [JSON-formatted file](https://auth0.com/docs/manage-users/user-migration/bulk-user-import-database-schema-and-examples) of the users you wish to import.\n\n1. To get the id for the Contoso-Users connection:\n    1. Go back to the tab where you are logged in to the Auth0 Dashboard.  \n    1. Go to **Authentication** > **Database** > **Contoso-Users**\n    1. Locate the label **Database Identifier** near the top of this screen. The value after it is the connection ID.\n    1. Copy the connection ID (**including** the prefix **con_**).\n    \n1. Go back to Management API Explorer tab and paste in the connection ID you copied in the **connection_id** field \n      \n1. Download the JSON file of users:\n    1. Go to this link and it will open in a new tab: https://raw.githubusercontent.com/auth0/auth0-product-education-labs/main/data/bulk-import-users.json\n    1. Save the file by going to **File** > **Save Page As** in your browser. Choose a location where you can easily locate the file.\n      \n1. Upload the file to the API Explorer and Issue the Request.\n    1. Select the **users** field to open your system's file explorer.\n    1. Go to where you saved **bulk-import-users.json** locally.\n    1. Select **Open** to upload the file to the Management API Explorer.\n    1. Select **Send** to send the request.\n    \n>Note that you should get a 202 HTTP response indicating that the job was accepted. Once the job completes, you will see that the users have been imported into the _Contoso-Users_ database connection. \n    ![return and view the users in the database](https://cdn.auth0.com/website/a0fun/v2/connections-24.gif)  \n    \n"
    },
    {
      "title": "Check that the users were imported",
      "description": "## Check that the users were successfully imported. \n1. From the Auth0 Dashboard, go to **User Management** > **Users***.\n1. You should see the newly imported users with a value of **Contoso-Users** under the *Connection* column.\n![return and view the users in the database](https://cdn.auth0.com/website/a0fun/v2/connections-23.gif)  \n\n1. Clean-up: Enable the User-Password-Authentication Database Connection again.\n    1. Go to **Applications** > **Applications** > **Lab: Web App**.\n    1. Select the **Connections** tab.\n    1. Toggle on **User-Password-Authentication**.\n"
    },
    {
      "file": "",
      "line": 1,
      "title": "Recap",
      "description": "## Recap\n\nIn this lab you:\n\n*   Enabled GitHub as a social connection for your Web App.\n*   Created a new Auth0 Database\n*   Added the User Import / Export Extension to your tenant\n*   Imported new users to the new database using bulk migration/user import."
    }
  ]
}
